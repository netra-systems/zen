# Development Environment Configuration
# =====================================
# IMPORTANT: For database connections, use Terraform-managed infrastructure
# Run: cd terraform-dev-postgres && terraform apply
# This will generate .env.development.local with all database credentials
# =====================================

ENVIRONMENT=development

# OAuth Client for Development
GOOGLE_OAUTH_CLIENT_ID_DEV=your-dev-client-id-here
GOOGLE_OAUTH_CLIENT_SECRET_DEV=your-dev-client-secret-here

# Development Features
ALLOW_DEV_LOGIN=true
ALLOW_MOCK_AUTH=true

# Frontend URLs
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Database Configuration
# NOTE: These are default values. Run terraform to get actual credentials
# The terraform setup will create .env.development.local with secure passwords
DATABASE_URL=postgresql+asyncpg://postgres:TERRAFORM_GENERATED@localhost:5432/netra_dev
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=netra_dev
POSTGRES_USER=postgres

# Redis Configuration  
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379

# ClickHouse Configuration
CLICKHOUSE_URL=clickhouse://default:netra_dev_password@localhost:9000/netra_dev
CLICKHOUSE_HOST=localhost
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000
CLICKHOUSE_DB=netra_dev
CLICKHOUSE_USER=default

# Security (development only - will be replaced by terraform)
SECRET_KEY=dev-secret-key-terraform-will-replace
JWT_SECRET_KEY=dev-jwt-secret-terraform-will-replace

# Development Mode Flags
DEBUG=true
RELOAD=true
LOG_LEVEL=DEBUG

# Performance Settings
MAX_CONNECTIONS=100
POOL_SIZE=20
POOL_OVERFLOW=10

# WebSocket Settings
WS_PING_INTERVAL=30
WS_PING_TIMEOUT=10

# Testing Database
TEST_DATABASE_URL=postgresql+asyncpg://postgres:TERRAFORM_GENERATED@localhost:5432/netra_test