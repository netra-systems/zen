# Netra Test Environment Configuration Template
# CRITICAL: Each environment MUST have separate OAuth credentials to prevent credential leakage
# Last Updated: 2025-09-05

# ============================================
# ENVIRONMENT IDENTIFICATION
# ============================================
ENVIRONMENT=test

# ============================================
# CRITICAL OAUTH CONFIGURATION - TEST
# ============================================
# SECURITY: Never use production OAuth credentials in test environment
GOOGLE_OAUTH_CLIENT_ID_TEST=your-test-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET_TEST=your-test-client-secret-replace-me

# GitHub OAuth (if enabled)
GITHUB_CLIENT_ID_TEST=your-test-github-client-id
GITHUB_CLIENT_SECRET_TEST=your-test-github-client-secret

# ============================================
# JWT CONFIGURATION - TEST
# ============================================
JWT_SECRET_KEY=test-jwt-secret-key-minimum-32-characters-replace-me
SECRET_KEY=test-secret-key-minimum-32-characters-replace-me

# ============================================
# DATABASE CONFIGURATION - TEST  
# ============================================
# Component-based configuration (PREFERRED)
POSTGRES_HOST=localhost
POSTGRES_PORT=5434
POSTGRES_DB=netra_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test_password

# Database URL is now built from POSTGRES_* variables above

# ============================================
# REDIS CONFIGURATION - TEST
# ============================================
# Component-based configuration (PREFERRED)
REDIS_HOST=localhost
REDIS_PORT=6381
REDIS_DB=0
REDIS_PASSWORD=

# Legacy REDIS_URL (DEPRECATED - will be removed in v2.0.0)
# System will auto-construct from components above if not provided
# REDIS_URL=redis://localhost:6381/0

# ============================================
# SERVICE URLS - TEST
# ============================================
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
AUTH_SERVICE_URL=http://localhost:8081
AUTH_REDIRECT_URI=http://localhost:8081/auth/callback
AUTH_CALLBACK_URL=http://localhost:8081/auth/callback

# ============================================
# LLM CONFIGURATION - TEST
# ============================================
# Test environment can use mock/placeholder values
ANTHROPIC_API_KEY=sk-ant-test-placeholder
OPENAI_API_KEY=sk-test-placeholder
GEMINI_API_KEY=test-gemini-key
PERPLEXITY_API_KEY=test-perplexity-key

# ============================================
# OPTIONAL SERVICES - TEST
# ============================================
# These can be left empty in test environment
SLACK_WEBHOOK_URL=
SLACK_CHANNEL=
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=netra-test
JIRA_API_TOKEN=
MIXPANEL_PROJECT_TOKEN=
SENTRY_DSN=

# ============================================
# DEVELOPMENT FLAGS - TEST
# ============================================
DEBUG=true
LOG_LEVEL=DEBUG
ALLOW_DEV_OAUTH_SIMULATION=false
RATE_LIMIT_ENABLED=false

# ============================================
# NOTES
# ============================================
# 1. This is a template file - copy to .env.test and fill in actual values
# 2. NEVER commit actual credentials to version control
# 3. Each environment MUST have separate OAuth credentials
# 4. Test environment uses different ports to avoid conflicts with dev
# 5. See CONFIG_REGRESSION_PREVENTION_PLAN.md for migration guidance