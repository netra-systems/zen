# Netra Backend Requirements - Modernized and Audited
# Report Date: 2025-09-01

# --- Core Web Framework ---
# FastAPI is the core web framework. Upgraded to a version compatible with Pydantic v2 and modern Starlette.
fastapi>=0.116.1
# Starlette is the underlying ASGI framework for FastAPI. Version is managed by FastAPI's dependencies.
starlette>=0.47.3

# --- ASGI Server ---
# uvicorn is the ASGI server for running FastAPI in production.
uvicorn[standard]>=0.35.0
# gunicorn provides process management for uvicorn in production environments.
gunicorn>=23.0.0

# --- Database ---
# SQLAlchemy 2.0 is used for database ORM functionality with full async support.
sqlalchemy>=2.0.43
# SQLModel provides Pydantic integration with SQLAlchemy models.
sqlmodel>=0.0.24
# Alembic handles database migrations for SQLAlchemy.
alembic>=1.16.4
# PostgreSQL adapter for SQLAlchemy
asyncpg>=0.30.0
# PostgreSQL driver with SSL support for both sync and async operations.
psycopg2-binary>=2.9.10

# --- Authentication ---
# PyJWT handles JWT token operations with cryptographic extensions.
PyJWT[cryptography]>=2.10.1
# Argon2 for password hashing (more secure than bcrypt).
argon2-cffi>=25.1.0
# bcrypt for legacy password compatibility if needed.
bcrypt>=4.3.0

# --- Validation ---
# Pydantic v2 for data validation and serialization. Must be compatible with FastAPI.
pydantic>=2.11.7
# Email validation utilities.
email-validator>=2.3.0

# --- AI/ML Dependencies ---
# OpenAI Python client for GPT integrations
openai>=1.101.0
# Anthropic Python client for Claude API
anthropic>=0.64.0
# Google Generative AI client
google-genai>=1.32.0
# LangChain for agent frameworks
langchain-core>=0.3.75
langchain-community>=0.3.28
langchain-google-genai>=2.1.10
# Numerical computing
numpy>=2.3.2
# Data manipulation and analysis
pandas>=2.3.2
# FastMCP for Model Context Protocol
fastmcp==2.11.3

# --- Caching ---
# Redis client for caching and session storage.
redis>=6.4.0
# In-memory caching utilities
cachetools==5.5.0

# --- HTTP Client ---
# Modern HTTP client for API calls with async support.
httpx>=0.28.1
# Alternative HTTP client for specific use cases
aiohttp>=3.12.15

# --- Configuration & Environment ---
# Loads environment variables from .env files.
python-dotenv>=1.1.1
# YAML parsing for configuration files.
pyyaml>=6.0.2
# Secure cookie signing.
itsdangerous>=2.2.0

# --- File Handling ---
# Handles multipart form data for file uploads.
python-multipart>=0.0.20

# --- Utilities ---
# Date and time utilities beyond standard library.
python-dateutil>=2.9.0.post0
# Rich console output for development and debugging.
rich>=14.1.0
# Retry logic for unreliable operations.
tenacity>=9.1.2
# Runtime type checking
beartype>=0.21.0
# System and process utilities
psutil>=7.0.0
# Fake data generation for testing
Faker>=37.6.0

# --- Security ---
# Industry-standard cryptographic library.
cryptography>=45.0.6

# --- Observability ---
# Structured logging
loguru>=0.7.3
# Metrics collection
prometheus-client>=0.22.1
# OpenTelemetry for distributed tracing
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp==1.24.0
opentelemetry-exporter-jaeger==1.21.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-instrumentation-redis==0.45b0
opentelemetry-instrumentation-requests==0.45b0
opentelemetry-instrumentation-sqlalchemy==0.45b0

# --- Analytics ---
# ClickHouse drivers for analytics database
clickhouse-driver>=0.2.9
clickhouse-connect>=0.8.18
# Compression for ClickHouse data
lz4>=4.4.4

# --- Google Cloud ---
# Google Cloud logging integration.
google-cloud-logging>=3.12.1
# Google Cloud Secret Manager for secure secret storage.
google-cloud-secret-manager>=2.20.0
# Cloud SQL Python connector for secure database connections.
cloud-sql-python-connector[asyncpg]>=1.18.4

# --- WebSocket ---
# Modern WebSocket library with full async support.
websockets>=15.0.1

# --- Testing ---
# The standard testing framework for Python.
pytest>=8.4.1
# Pytest plugin for testing asyncio code.
pytest-asyncio>=1.1.0
# Coverage plugin for pytest to measure code coverage.
pytest-cov>=6.0.0