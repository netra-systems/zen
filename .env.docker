# Docker Compose Port Configuration
# This file manages port allocations to avoid conflicts between different environments

# Production/Main Environment Ports (docker-compose.yml)
POSTGRES_PORT=5432
REDIS_PORT=6379
CLICKHOUSE_HTTP_PORT=8125  # Changed from 8123 to avoid conflict
CLICKHOUSE_TCP_PORT=9002   # Changed from 9000 to avoid conflict
BACKEND_PORT=8000
AUTH_PORT=8081
FRONTEND_PORT=3000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001

# Development Environment Ports (docker-compose.dev.yml)
DEV_POSTGRES_PORT=5433
DEV_REDIS_PORT=6380
DEV_CLICKHOUSE_HTTP_PORT=8124
DEV_CLICKHOUSE_TCP_PORT=9001
DEV_BACKEND_PORT=8002
DEV_AUTH_PORT=8082
DEV_FRONTEND_PORT=3002

# Test Environment Ports (docker-compose.test.yml)
TEST_POSTGRES_PORT=5434
TEST_REDIS_PORT=6381
TEST_CLICKHOUSE_HTTP_PORT=8123
TEST_CLICKHOUSE_TCP_PORT=9000
TEST_BACKEND_PORT=8003
TEST_AUTH_PORT=8083
TEST_FRONTEND_PORT=3003

# Port Allocation Summary:
# PostgreSQL: 5432 (prod), 5433 (dev), 5434 (test)
# Redis: 6379 (prod), 6380 (dev), 6381 (test)
# ClickHouse HTTP: 8125 (prod), 8124 (dev), 8123 (test)
# ClickHouse TCP: 9002 (prod), 9001 (dev), 9000 (test)
# Backend: 8000 (prod), 8002 (dev), 8003 (test)
# Auth: 8081 (prod), 8082 (dev), 8083 (test)
# Frontend: 3000 (prod), 3002 (dev), 3003 (test)