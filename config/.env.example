# Google OAuth Configuration - Environment-specific
# To set up Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create OAuth 2.0 credentials for each environment
# 3. Add authorized redirect URIs for each environment:
#    - Development: http://localhost:8000/auth/callback, http://localhost:3000/auth/callback
#    - Staging: https://auth.netra-staging.app/auth/callback, https://netra-staging.app/auth/callback
#    - Production: https://auth.netrasystems.ai/auth/callback, https://netra.app/auth/callback
# 4. Copy the Client ID and Client Secret for your environment below

# Development OAuth Credentials
GOOGLE_OAUTH_CLIENT_ID_DEVELOPMENT=your_dev_google_client_id_here
GOOGLE_OAUTH_CLIENT_SECRET_DEVELOPMENT=your_dev_google_client_secret_here

# Staging OAuth Credentials
GOOGLE_OAUTH_CLIENT_ID_STAGING=your_staging_google_client_id_here
GOOGLE_OAUTH_CLIENT_SECRET_STAGING=your_staging_google_client_secret_here

# Other API Keys
GEMINI_API_KEY=your_gemini_api_key_here

# ClickHouse Configuration (Optional for local development)
CLICKHOUSE_HOST=xedvrr4c3r.us-central1.gcp.clickhouse.cloud
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=your_password_here
CLICKHOUSE_DB=default

# Environment Configuration
environment=development

# E2E Test Configuration
# Used for automated testing bypass authentication on staging
# For development: use placeholder value
# For staging: this will be set from Google Secrets Manager
E2E_OAUTH_SIMULATION_KEY=your_e2e_oauth_bypass_key_here